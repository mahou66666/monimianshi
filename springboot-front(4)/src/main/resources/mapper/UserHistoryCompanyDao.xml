<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootfront.dao.UserHistoryCompanyDao">

    <resultMap type="com.example.springbootfront.entity.UserHistoryCompany" id="UserHistoryCompanyMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="companyId" column="company_id" jdbcType="INTEGER"/>
        <result property="companyContent" column="company_content" jdbcType="VARCHAR"/>
        <result property="isDeleted" column="is_deleted" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserHistoryCompanyMap">
        select id,
               user_id,
               company_id,
               company_content,
               is_deleted,
               create_time
        from user_history_company
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserHistoryCompanyMap">
        select
        id, user_id, company_id, company_content, is_deleted, create_time
        from user_history_company
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="companyId != null">
                and company_id = #{companyId}
            </if>
            <if test="companyContent != null and companyContent != ''">
                and company_content = #{companyContent}
            </if>
            <if test="isDeleted != null">
                and is_deleted = #{isDeleted}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from user_history_company
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="companyId != null">
                and company_id = #{companyId}
            </if>
            <if test="companyContent != null and companyContent != ''">
                and company_content = #{companyContent}
            </if>
            <if test="isDeleted != null">
                and is_deleted = #{isDeleted}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into user_history_company(user_id, company_id, company_content, is_deleted, create_time)
        values (#{userId}, #{companyId}, #{companyContent}, #{isDeleted}, #{createTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user_history_company(user_id, company_id, company_content, is_deleted, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.companyId}, #{entity.companyContent}, #{entity.isDeleted}, #{entity.createTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user_history_company(user_id, company_id, company_content, is_deleted, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.companyId}, #{entity.companyContent}, #{entity.isDeleted}, #{entity.createTime})
        </foreach>
        on duplicate key update
        user_id = values(user_id),
        company_id = values(company_id),
        company_content = values(company_content),
        is_deleted = values(is_deleted),
        create_time = values(create_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user_history_company
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="companyId != null">
                company_id = #{companyId},
            </if>
            <if test="companyContent != null and companyContent != ''">
                company_content = #{companyContent},
            </if>
            <if test="isDeleted != null">
                is_deleted = #{isDeleted},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from user_history_company
        where id = #{id}
    </delete>

</mapper>

