<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootfront.dao.UserHistoryJdDao">

    <resultMap type="com.example.springbootfront.entity.UserHistoryJd" id="UserHistoryJdMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="jdId" column="jd_id" jdbcType="INTEGER"/>
        <result property="jdContent" column="jd_content" jdbcType="VARCHAR"/>
        <result property="isDeleted" column="is_deleted" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserHistoryJdMap">
        select id,
               user_id,
               jd_id,
               jd_content,
               is_deleted,
               create_time
        from user_history_jd
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserHistoryJdMap">
        select
        id, user_id, jd_id, jd_content, is_deleted, create_time
        from user_history_jd
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="jdId != null">
                and jd_id = #{jdId}
            </if>
            <if test="jdContent != null and jdContent != ''">
                and jd_content = #{jdContent}
            </if>
            <if test="isDeleted != null">
                and is_deleted = #{isDeleted}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from user_history_jd
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="jdId != null">
                and jd_id = #{jdId}
            </if>
            <if test="jdContent != null and jdContent != ''">
                and jd_content = #{jdContent}
            </if>
            <if test="isDeleted != null">
                and is_deleted = #{isDeleted}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into user_history_jd(user_id, jd_id, jd_content, is_deleted, create_time)
        values (#{userId}, #{jdId}, #{jdContent}, #{isDeleted}, #{createTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user_history_jd(user_id, jd_id, jd_content, is_deleted, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.jdId}, #{entity.jdContent}, #{entity.isDeleted}, #{entity.createTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user_history_jd(user_id, jd_id, jd_content, is_deleted, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.jdId}, #{entity.jdContent}, #{entity.isDeleted}, #{entity.createTime})
        </foreach>
        on duplicate key update
        user_id = values(user_id),
        jd_id = values(jd_id),
        jd_content = values(jd_content),
        is_deleted = values(is_deleted),
        create_time = values(create_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user_history_jd
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="jdId != null">
                jd_id = #{jdId},
            </if>
            <if test="jdContent != null and jdContent != ''">
                jd_content = #{jdContent},
            </if>
            <if test="isDeleted != null">
                is_deleted = #{isDeleted},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from user_history_jd
        where id = #{id}
    </delete>

</mapper>

