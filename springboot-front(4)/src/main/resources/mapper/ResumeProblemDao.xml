<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootfront.dao.ResumeProblemDao">

    <resultMap type="com.example.springbootfront.entity.ResumeProblem" id="ResumeProblemMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="resumeId" column="resume_id" jdbcType="INTEGER"/>
        <result property="problemTitle" column="problem_title" jdbcType="VARCHAR"/>
        <result property="problemDesc" column="problem_desc" jdbcType="VARCHAR"/>
        <result property="priority" column="priority" jdbcType="VARCHAR"/>
        <result property="suggestion" column="suggestion" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ResumeProblemMap">
        select id,
               resume_id,
               problem_title,
               problem_desc,
               priority,
               suggestion,
               status,
               create_time,
               update_time
        from resume_problem
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ResumeProblemMap">
        select
        id, resume_id, problem_title, problem_desc, priority, suggestion, status, create_time, update_time
        from resume_problem
        <where>
            <if test="resumeProblem.id != null">
                and id = #{resumeProblem.id}
            </if>
            <if test="resumeProblem.resumeId != null">
                and resume_id = #{resumeProblem.resumeId}
            </if>
            <if test="resumeProblem.problemTitle != null and resumeProblem.problemTitle != ''">
                and problem_title = #{resumeProblem.problemTitle}
            </if>
            <if test="resumeProblem.problemDesc != null and resumeProblem.problemDesc != ''">
                and problem_desc = #{resumeProblem.problemDesc}
            </if>
            <if test="resumeProblem.priority != null and resumeProblem.priority != ''">
                and priority = #{resumeProblem.priority}
            </if>
            <if test="resumeProblem.suggestion != null and resumeProblem.suggestion != ''">
                and suggestion = #{resumeProblem.suggestion}
            </if>
            <if test="resumeProblem.status != null">
                and status = #{resumeProblem.status}
            </if>
            <if test="resumeProblem.createTime != null">
                and create_time = #{resumeProblem.createTime}
            </if>
            <if test="resumeProblem.updateTime != null">
                and update_time = #{resumeProblem.updateTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from resume_problem
        <where>
            <if test="resumeProblem.id != null">
                and id = #{resumeProblem.id}
            </if>
            <if test="resumeProblem.resumeId != null">
                and resume_id = #{resumeProblem.resumeId}
            </if>
            <if test="resumeProblem.problemTitle != null and resumeProblem.problemTitle != ''">
                and problem_title = #{resumeProblem.problemTitle}
            </if>
            <if test="resumeProblem.problemDesc != null and resumeProblem.problemDesc != ''">
                and problem_desc = #{resumeProblem.problemDesc}
            </if>
            <if test="resumeProblem.priority != null and resumeProblem.priority != ''">
                and priority = #{resumeProblem.priority}
            </if>
            <if test="resumeProblem.suggestion != null and resumeProblem.suggestion != ''">
                and suggestion = #{resumeProblem.suggestion}
            </if>
            <if test="resumeProblem.status != null">
                and status = #{resumeProblem.status}
            </if>
            <if test="resumeProblem.createTime != null">
                and create_time = #{resumeProblem.createTime}
            </if>
            <if test="resumeProblem.updateTime != null">
                and update_time = #{resumeProblem.updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into resume_problem(resume_id, problem_title, problem_desc, priority, suggestion, status, create_time,
                                   update_time)
        values (#{resumeId}, #{problemTitle}, #{problemDesc}, #{priority}, #{suggestion}, #{status}, #{createTime},
                #{updateTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into resume_problem(resume_id, problem_title, problem_desc, priority, suggestion, status, create_time,
        update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.resumeId}, #{entity.problemTitle}, #{entity.problemDesc}, #{entity.priority},
            #{entity.suggestion}, #{entity.status}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into resume_problem(resume_id, problem_title, problem_desc, priority, suggestion, status, create_time,
        update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.resumeId}, #{entity.problemTitle}, #{entity.problemDesc}, #{entity.priority},
            #{entity.suggestion}, #{entity.status}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
        on duplicate key update
        resume_id = values(resume_id),
        problem_title = values(problem_title),
        problem_desc = values(problem_desc),
        priority = values(priority),
        suggestion = values(suggestion),
        status = values(status),
        create_time = values(create_time),
        update_time = values(update_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update resume_problem
        <set>
            <if test="resumeId != null">
                resume_id = #{resumeId},
            </if>
            <if test="problemTitle != null and problemTitle != ''">
                problem_title = #{problemTitle},
            </if>
            <if test="problemDesc != null and problemDesc != ''">
                problem_desc = #{problemDesc},
            </if>
            <if test="priority != null and priority != ''">
                priority = #{priority},
            </if>
            <if test="suggestion != null and suggestion != ''">
                suggestion = #{suggestion},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from resume_problem
        where id = #{id}
    </delete>

    <delete id="deleteByResumeId">
        delete
        from resume_problem
        where resume_id = #{resumeId}
    </delete>

</mapper>

