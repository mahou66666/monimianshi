<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootfront.dao.UserEducationDao">

    <resultMap type="com.example.springbootfront.entity.UserEducation" id="UserEducationMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="schoolId" column="school_id" jdbcType="VARCHAR"/>
        <result property="major" column="major" jdbcType="VARCHAR"/>
        <result property="degree" column="degree" jdbcType="VARCHAR"/>
        <result property="startTime" column="start_time" jdbcType="TIMESTAMP"/>
        <result property="endTime" column="end_time" jdbcType="TIMESTAMP"/>
        <result property="intro" column="intro" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserEducationMap">
        select id,
               user_id,
               school_id,
               major,
               degree,
               start_time,
               end_time,
               intro,
               content,
               create_time,
               update_time
        from user_education
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserEducationMap">
        select
        id, user_id, school_id, major, degree, start_time, end_time, intro, content, create_time, update_time
        from user_education
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="schoolId != null and schoolId != ''">
                and school_id = #{schoolId}
            </if>
            <if test="major != null and major != ''">
                and major = #{major}
            </if>
            <if test="degree != null and degree != ''">
                and degree = #{degree}
            </if>
            <if test="startTime != null">
                and start_time = #{startTime}
            </if>
            <if test="endTime != null">
                and end_time = #{endTime}
            </if>
            <if test="intro != null and intro != ''">
                and intro = #{intro}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from user_education
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="schoolId != null and schoolId != ''">
                and school_id = #{schoolId}
            </if>
            <if test="major != null and major != ''">
                and major = #{major}
            </if>
            <if test="degree != null and degree != ''">
                and degree = #{degree}
            </if>
            <if test="startTime != null">
                and start_time = #{startTime}
            </if>
            <if test="endTime != null">
                and end_time = #{endTime}
            </if>
            <if test="intro != null and intro != ''">
                and intro = #{intro}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into user_education(user_id, school_id, major, degree, start_time, end_time, intro, content, create_time,
                                   update_time)
        values (#{userId}, #{schoolId}, #{major}, #{degree}, #{startTime}, #{endTime}, #{intro}, #{content},
                #{createTime}, #{updateTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user_education(user_id, school_id, major, degree, start_time, end_time, intro, content, create_time,
        update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.schoolId}, #{entity.major}, #{entity.degree}, #{entity.startTime},
            #{entity.endTime}, #{entity.intro}, #{entity.content}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into user_education(user_id, school_id, major, degree, start_time, end_time, intro, content, create_time,
        update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userId}, #{entity.schoolId}, #{entity.major}, #{entity.degree}, #{entity.startTime},
            #{entity.endTime}, #{entity.intro}, #{entity.content}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
        on duplicate key update
        user_id = values(user_id),
        school_id = values(school_id),
        major = values(major),
        degree = values(degree),
        start_time = values(start_time),
        end_time = values(end_time),
        intro = values(intro),
        content = values(content),
        create_time = values(create_time),
        update_time = values(update_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user_education
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="schoolId != null and schoolId != ''">
                school_id = #{schoolId},
            </if>
            <if test="major != null and major != ''">
                major = #{major},
            </if>
            <if test="degree != null and degree != ''">
                degree = #{degree},
            </if>
            <if test="startTime != null">
                start_time = #{startTime},
            </if>
            <if test="endTime != null">
                end_time = #{endTime},
            </if>
            <if test="intro != null and intro != ''">
                intro = #{intro},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from user_education
        where id = #{id}
    </delete>

</mapper>

